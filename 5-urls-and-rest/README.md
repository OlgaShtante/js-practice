# URLs & REST

Parsing URL query strings and issuing REST requests.

- [`0-parse-url-params`](0-parse-url-params): extract query params by string
  splitting.
- [`1-url-parser-class`](1-url-parser-class): the same in an ES class, plus a
  regular-expression approach.
- [`2-rest-http-requests`](2-rest-http-requests): a `.http` file exercising a mock
  REST API (GET/POST).
