# Objects & Prototypes

The same classroom domain modelled several ways, building up from plain objects
to constructors, ES modules, and prototype-based inheritance.

- [`0-classroom-object-literals`](0-classroom-object-literals) — object literals with
  method shorthand.
- [`1-nested-function-scope`](1-nested-function-scope) — constructor functions in
  nested scope.
- [`2-classroom-oop-model`](2-classroom-oop-model) — the same, split across ES
  modules.
- [`3-prototype-inheritance`](3-prototype-inheritance) — behaviour on
  `Constructor.prototype`.
- [`4-prototype-chain`](4-prototype-chain) — a three-level chain via `Object.create`.
