# vRUSLO — Forms / JSON server (project links)

**Not a standalone code exercise.** A link page for the **vRUSLO** project's
forms-with-a-custom-JSON-server stage: `index.html` links to the custom JSON
server and to the project on StackBlitz.
