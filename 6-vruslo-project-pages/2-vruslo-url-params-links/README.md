# vRUSLO â URL-parsing stage (project links)

**Not a standalone code exercise.** A link/landing page for the **vRUSLO**
project; `index.html` links out to Google Sheets (current status, project
report, requirements).

> References a `../src/modules/main.js` that does **not** exist here â the script
> tag is dead. The actual code lived in the separate vRUSLO project.
