# vRUSLO: Promises stage (project links)

**Not a standalone code exercise.** A link/landing page for the course's larger
**vRUSLO** project at the Promises stage. `index.html` points to external Google
Sheets (requirements, current status, project report).

> The page references `../src/modules/main.js`, which does **not** exist in this
> folder, so the script tag is dead. The real code lived in the separate vRUSLO
> project.
